name: Build and deploy JAR app to Azure Web App - postech-fiap-app1

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'microsoft'


      - name: Find pom.xml
        run: |
          pwd
          ls -la
          echo "---- poms ----"
          find . -maxdepth 3 -name pom.xml -print

      - name: Build with Maven (complete/)
        working-directory: complete
        run: mvn -B -DskipTests clean package

      - name: Upload artifact (JAR)
        uses: actions/upload-artifact@v4
        with:
          name: java-app
          path: complete/target/*.jar

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: java-app

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_FD4FF28647AE49A892F21DF2C5CFDA0F }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_ECA018E6CFB648E6A6233B552BD79EE3 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_ED65EEE8E7AB4142A2A4F46A33CCF4F6 }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'postech-fiap-app1'
          slot-name: 'Production'
          package: '*.jar'

